The raw data:
train_feature, train_label, validation_feature, validation_label

After scaled:
scaled_train_feature, scaled_validation_feature

After scaled and outlier detection(whole data set):
X_train_after_detection, y_train_after_detection
